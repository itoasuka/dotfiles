snippet react
  var React = require('react');
  
  var ${1:name} = React.createClass({
    name: '$1',
    
    render() {
      return (
        /* jshint ignore:start */
        ${0}
        /* jshint ignore:end */
      );
    }
  });
  
  module.exports = $1;
